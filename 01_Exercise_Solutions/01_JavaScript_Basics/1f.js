document.body.innerHTML = 'Good morning!';

document.body.innerHTML = "Good morning!";

// Note:
// Both single ('') and double ("") quotes work in JavaScript.
// This repository uses single quotes by convention.
